services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: hyperhive-website:latest
    container_name: hyperhive-website
    ports:
      - "3000:3000"
    restart: unless-stopped
